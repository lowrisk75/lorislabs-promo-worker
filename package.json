{
  "name": "lorislabs-promo-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker that distributes one-time promo codes per channel for LorisLabs apps",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "wrangler": "^3.99.0"
  }
}
